# Redis — uses docker-compose internal network by default
# Only override if running Redis externally
# REDIS_URL=redis://redis:6379

# Required: protect the check-deadlines HTTP endpoint
CRON_SECRET=your-random-secret-here

# Optional: restrict CORS to a specific origin (defaults to http://localhost:5173)
# CORS_ORIGIN=https://your-domain.example.com
